spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: ms-customer-service

server:
  port: 8090

customer:
  config:
    personal:
      vip:
        avgDailyMinimumAmount: 100
